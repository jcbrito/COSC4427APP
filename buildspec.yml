version: 0.2

phases:
  pre_build:
    commands:
      - echo "Pre-build phase"
      
  
  build:
    commands:
      - echo "Build phase"
      - npm build
     
  
  post_build:
    commands:
      - echo "All good"

  reports:
   jasmine_reports:
    files:
      -  testReport.json
    file-format: JunitXml
    base-directory: elasticbeanstalk-us-east-1-840375668770/tests


   


