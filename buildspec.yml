version: 0.2

phases:
  pre_build:
    commands:
      - echo "Pre-build phase $USERNAME"
      
  
  build:
    commands:
      - echo "Build phase"
      - npm build
     
  
  post_build:
    commands:
      - echo "All good"


   


