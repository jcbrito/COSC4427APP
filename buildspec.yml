version: 0.2

phases:
  pre_build:
    commands:
      - echo "Pre-build phase"
      - npm install
  
  build:
    commands:
      - echo "Build phase"
      - npm build
     
  
  post_build:
    commands:
      - echo "All good"

reports:
  jasmine_reports:
    files:
      - report.json
    file-format: JunitXml
   


